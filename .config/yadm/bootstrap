#!/bin/bash
# yadm clone 後に自動実行される。Homebrew のパッケージを Brewfile から揃える。
set -eu

if command -v brew >/dev/null 2>&1; then
  echo "==> brew bundle"
  brew bundle --file="$HOME/.Brewfile"
else
  echo "Homebrew が見つかりません。インストール後に brew bundle --file=~/.Brewfile を実行してください。"
fi

echo "==> bootstrap done"
